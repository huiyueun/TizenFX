using System.Collections.Generic;
using System.Linq;
using Tizen.NUI;

namespace Space.BuildingTool.ExtensionMethod
{
    internal static class TransformExtension
    {
        #region Local / World Position
        /*
        public static List<Vector3> LocalToWorldVertices(this Transform transform, IList<Vector3> vertices)
        {
            return vertices.Select((vec) => transform.TransformPoint(vec)).ToList();
        }

        public static List<Vector3> WorldToLocalVertices(this Transform transform, IList<Vector3> vertices)
        {
            return vertices.Select((vec) => transform.InverseTransformPoint(vec)).ToList();
        }
        */
        #endregion
    }
}
